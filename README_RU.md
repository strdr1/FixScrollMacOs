# RDP Scroll Fixer для macOS

Простое приложение, которое исправляет дерганую и неработающую прокрутку в Microsoft Remote Desktop (RDP) на Mac.

---

## 📥 1. Установка
1. Скачайте файл **`RDP_Scroll_Fixer_Installer.dmg`**.
2. Откройте его двойным кликом.
3. Перетащите иконку приложения в папку **Applications** (Программы).

## 🚀 2. Первый запуск
**Важно!** Так как приложение не из App Store, macOS может его заблокировать при обычном клике.

1. Зайдите в папку **Программы**.
2. Найдите **RDP Scroll Fixer**.
3. Нажмите на него **ПРАВОЙ** кнопкой мыши.
4. Выберите **Открыть** в меню.
5. В появившемся окне нажмите кнопку **Открыть**.

*(Это нужно сделать только один раз).*

## 🔐 3. Выдача прав
Чтобы приложение могло исправлять прокрутку, ему нужен доступ.

1. При запуске появится окно с просьбой дать права **Accessibility** (Универсальный доступ).
2. Нажмите кнопку перехода в настройки.
3. В списке программ найдите **RDP Scroll Fixer** и **поставьте галочку**.
   * *Если галочка уже стоит, но не работает — снимите её и поставьте заново.*

## ⚙️ Использование
Приложение висит в верхней строке меню (иконка или название).
* **Active** — Включение/выключение исправления.
* **Sensitivity** — Настройка чувствительности (1 — плавно, 5 — быстро).
* **Autostart** — Запускать вместе с системой.

Просто запустите приложение, откройте RDP, и прокрутка должна работать плавно (как двумя пальцами на тачпаде).

## 📝 Логи (если что-то не работает)
Приложение пишет простой текстовый лог, чтобы можно было понять, что происходит.
Файл находится в вашей домашней папке:
`~/rdp_scroll_fixer.log` (или `/Users/ВАШЕ_ИМЯ/rdp_scroll_fixer.log`)

Если возникли проблемы, можно открыть этот файл и посмотреть, есть ли там ошибки или события прокрутки.

## ⚠️ Возможные проблемы и решения

### Ошибка «Файл поврежден» или «Operation not permitted»
Если macOS говорит, что файл поврежден, или в терминале пишет `zsh: operation not permitted`:
1. Откройте **Терминал** (Terminal).
2. Вставьте команду и нажмите Enter:
   ```bash
   xattr -cr "/Applications/RDP Scroll Fixer.app"
   ```
3. После этого приложение запустится.

### Программа запущена, но прокрутка не работает
Иногда macOS «забывает» выданные права.
1. Зайдите в **Системные настройки** -> **Конфиденциальность** -> **Универсальный доступ**.
2. Удалите `RDP Scroll Fixer` из списка (кнопкой «минус»).
3. Запустите приложение заново и снова выдайте права.

---

## 🛠 Сборка из исходного кода (Терминал)
Если вы скачали исходный код и хотите собрать приложение сами:

1. Откройте терминал в папке проекта.
   * *Совет: напишите в терминале `cd ` (с пробелом), перетащите папку проекта в окно терминала и нажмите Enter.*
2. Запустите команду сборки:
   ```bash
   sh build_installer.sh
   ```
3. Скрипт сам создаст виртуальное окружение, установит библиотеки и соберет `.dmg`.
4. Готовый файл будет лежать в папке `dist/`:
   `dist/RDP_Scroll_Fixer_Installer.dmg`
