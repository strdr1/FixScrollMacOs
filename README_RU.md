# RDP Scroll Fixer для macOS

Простое приложение, которое исправляет дерганую и неработающую прокрутку в Microsoft Remote Desktop (RDP) на Mac.

---

## 📥 1. Установка
1. Скачайте файл **`RDP_Scroll_Fixer_Installer.dmg`**.
2. Откройте его двойным кликом.
3. Перетащите иконку приложения в папку **Applications** (Программы).

## 🚀 2. Первый запуск
**Важно!** Так как приложение не из App Store, macOS может его заблокировать при обычном клике.

1. Зайдите в папку **Программы**.
2. Найдите **RDP Scroll Fixer**.
3. Нажмите на него **ПРАВОЙ** кнопкой мыши.
4. Выберите **Открыть** в меню.
5. В появившемся окне нажмите кнопку **Открыть**.

*(Это нужно сделать только один раз).*

## 📝 Логи и диагностика

Если приложение работает, но скролл не исправляется, проверьте логи.

### Где найти логи?
Лог-файл находится в вашей домашней папке:
`~/rdp_scroll_fixer.log`
(Например: `/Users/username/rdp_scroll_fixer.log`)

### Как читать логи?
Вы можете открыть файл в TextEdit или следить за ним в реальном времени через терминал:
```bash
tail -f ~/rdp_scroll_fixer.log
```

### Примеры записей:

1. **Запуск приложения:**
   ```
   Application started
   ```

2. **Обнаружение скролла (Тачпад):**
   Если вы ведете двумя пальцами, вы должны увидеть:
   ```
   DETECTED: Continuous Scroll Event. App: com.microsoft.rdc.macos
   INPUT: DeltaY=-5 (Continuous)
   ```
   *Если этого нет, значит приложение не видит ваш скролл (проверьте права доступа!).*

3. **Исправление скролла (Action):**
   Когда накапливается достаточно движения, программа делает "клик" колесиком:
   ```
   ACTION: InputDelta=-10 -> Accum=-40 -> Steps=-1
   ```
   *Это значит, что скролл был преобразован в обычный "мышиный" скролл.*

4. **Неизвестная версия RDP:**
   Если вы видите это, значит у вас новая или бета-версия RDP:
   ```
   Potentially unsupported RDP app detected: com.microsoft.rdc.macos.new
   ```
   *Сообщите разработчику этот ID.*

## 🗑 Как удалить/обновить старую версию
Перед установкой новой версии лучше полностью удалить старую, чтобы не было конфликтов.

1. **Закройте программу:**
   * Нажмите на иконку (или название) в верхней строке меню -> **Quit** (Выход).
   * Если иконки нет, но программа работает: откройте «Мониторинг системы» (Activity Monitor), найдите `RDP Scroll Fixer` и завершите его.

2. **Удалите файл:**
   * Зайдите в папку **Программы** (Applications).
   * Перетащите `RDP Scroll Fixer` в корзину.
   * Очистите корзину.

3. **Сбросьте права (важно!):**
   * Зайдите в **Системные настройки** -> **Конфиденциальность и безопасность** -> **Универсальный доступ** (Accessibility).
   * Найдите `RDP Scroll Fixer` и нажмите кнопку **«-» (минус)**, чтобы удалить его из списка.

---

## 🚀 Установка новой версии (Полный гайд)

1. **Скачайте** новый `RDP_Scroll_Fixer_Installer.dmg`.
2. **Откройте** его и перетащите приложение в папку **Программы** (Applications).
3. **Снимите карантин (Обязательно!):**
   * Откройте **Терминал**.
   * Вставьте команду и нажмите Enter:
     ```bash
     xattr -cr "/Applications/RDP Scroll Fixer.app"
     ```
   * *(Команда ничего не ответит — это нормально).*
4. **Запустите приложение:**
   * Зайдите в Программы -> двойной клик по **RDP Scroll Fixer**.
5. **Дайте права:**
   * Появится окно с просьбой прав Accessibility.
   * Нажмите кнопку перехода в настройки.
   * **Включите галочку** напротив RDP Scroll Fixer.

Теперь все должно работать идеально.

## ⚙️ Использование
Приложение висит в верхней строке меню (иконка или название).
* **Active** — Включение/выключение исправления.
* **Sensitivity** — Настройка чувствительности (1 — плавно, 5 — быстро).
* **Autostart** — Запускать вместе с системой.

Просто запустите приложение, откройте RDP, и прокрутка должна работать плавно (как двумя пальцами на тачпаде).

## 📝 Логи (если что-то не работает)
Приложение пишет простой текстовый лог, чтобы можно было понять, что происходит.
Файл находится в вашей домашней папке:
`~/rdp_scroll_fixer.log` (или `/Users/ВАШЕ_ИМЯ/rdp_scroll_fixer.log`)

Если возникли проблемы, можно открыть этот файл и посмотреть, есть ли там ошибки или события прокрутки.

## ⚠️ Возможные проблемы и решения

### Ошибка «Файл поврежден» или «Operation not permitted»
Если macOS говорит, что файл поврежден, или в терминале пишет `zsh: operation not permitted`:
1. Откройте **Терминал** (Terminal).
2. Вставьте команду и нажмите Enter:
   ```bash
   xattr -cr "/Applications/RDP Scroll Fixer.app"
   ```
3. После этого приложение запустится.

### Программа запущена, но прокрутка не работает
Иногда macOS «забывает» выданные права.
1. Зайдите в **Системные настройки** -> **Конфиденциальность** -> **Универсальный доступ**.
2. Удалите `RDP Scroll Fixer` из списка (кнопкой «минус»).
3. Запустите приложение заново и снова выдайте права.

---

## 🛠 Сборка из исходного кода (Терминал)
Если вы скачали исходный код и хотите собрать приложение сами:

1. Откройте терминал в папке проекта.
   * *Совет: напишите в терминале `cd ` (с пробелом), перетащите папку проекта в окно терминала и нажмите Enter.*
2. Запустите команду сборки:
   ```bash
   sh build_installer.sh
   ```
3. Скрипт сам создаст виртуальное окружение, установит библиотеки и соберет `.dmg`.
4. Готовый файл будет лежать в папке `dist/`:
   `dist/RDP_Scroll_Fixer_Installer.dmg`
